{
  "bankId": "frontend-interview-gold-bank",
  "bankVersion": "1.0.0",
  "items": [
    {
      "competency": "template-observable-subscription",
      "difficultyBand": "foundation",
      "estimatedSeconds": 75,
      "format": "conceptual",
      "id": "int-angular-async-pipe-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-530e993124",
          "label": "Subscribe with `takeUntilDestroyed()`, assign each value to a field, and rely on that assignment to refresh the `OnPush` view."
        },
        {
          "id": "choice-f4d0721513",
          "label": "Read `price$` with `firstValueFrom()`, assign the result to a field, and call `markForCheck()` when the Promise settles."
        },
        {
          "id": "choice-1e010e05b8",
          "label": "Render `price$` through the `async` pipe, which marks the view on emissions and unsubscribes when the view is destroyed."
        }
      ],
      "prompt": "An `OnPush` price component renders a long-lived `price$` whose emissions occur after initial rendering. The template must show every emission, and the subscription must end when the view is destroyed. Which approach meets both requirements?",
      "revision": 3,
      "technology": "angular"
    },
    {
      "competency": "template-binding-direction",
      "difficultyBand": "foundation",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-angular-binding-direction-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-a726bda356",
          "label": "Property-bound state with a called event method: `<button [disabled]=\"isSaving\" (click)=\"save()\">Save</button>`."
        },
        {
          "id": "choice-e381124a64",
          "label": "Attribute-bound state with an executed event expression: `<button [attr.disabled]=\"isSaving\" (click)=\"save()\">Save</button>`."
        },
        {
          "id": "choice-a96f7261d6",
          "label": "Property-bound state with a passed method reference: `<button [disabled]=\"isSaving\" (click)=\"save\">Save</button>`."
        }
      ],
      "prompt": "A Save button must be disabled while `isSaving` is true and call the component's `save()` method when the user activates it. Which Angular template expresses both data flows correctly?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "code": {
        "language": "typescript",
        "runtime": "angular-22",
        "source": "ngOnInit() {\n  const badge = document.createElement('span');\n  badge.textContent = this.status;\n  this.host.nativeElement.appendChild(badge);\n}"
      },
      "competency": "ssr-hydration-dom-ownership",
      "difficultyBand": "foundation",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-angular-hydration-dom-mismatch-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-d5c03bc94f",
          "label": "Move the append to `ngAfterViewInit` and perform it through `Renderer2` after the first client render."
        },
        {
          "id": "choice-fe96112685",
          "label": "Render the badge in the component template so server and client produce one Angular-managed DOM structure."
        },
        {
          "id": "choice-b51a80fc34",
          "label": "Add `ngSkipHydration` to this component host and let Angular recreate that subtree only on the client."
        }
      ],
      "prompt": "An Angular SSR page reports DOM-mismatch errors after a component appends a status badge during initialization. The badge derives only from the component's `status` value, is required page markup, and its component must remain hydratable. Which fix restores hydration's ownership contract?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "code": {
        "language": "typescript",
        "runtime": "angular-17",
        "source": "@Component({\n  selector: 'user-card',\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  template: `{{ user.name }}`\n})\nexport class UserCardComponent {\n  @Input() user!: User;\n}\n\n// Normalized store\nrename(userId: string, nextName: string) {\n  const user = this.usersById[userId];\n  user.name = nextName;\n  this.selectedUser = user;\n}"
      },
      "competency": "onpush-input-identity",
      "difficultyBand": "foundation",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-angular-onpush-reference-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-1aabb13e93",
          "label": "Keep mutating the cached entity, but clone it inside the dashboard selector before passing it to matching cards."
        },
        {
          "id": "choice-4bd3139db7",
          "label": "Keep the cached entity reference and increment one version input passed to every card after any entity write."
        },
        {
          "id": "choice-c1d3f4d122",
          "label": "Replace the cached entity immutably and publish its new reference while preserving every unchanged entity reference."
        }
      ],
      "prompt": "A normalized Angular 17 store publishes entity references to several selectors. It mutates one cached user in place, so an `OnPush` card stays stale; profiling shows unchanged cards currently prune correctly. Updated consumers need one coherent snapshot while unaffected cards should keep skipping work. Which store contract fixes the boundary?",
      "revision": 4,
      "technology": "angular"
    },
    {
      "competency": "component-output-propagation",
      "difficultyBand": "core",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-angular-output-nonbubbling-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-9877835aea",
          "label": "Move the shell listener to a higher ancestor and rely on the editor output to propagate through component boundaries."
        },
        {
          "id": "choice-3b04614739",
          "label": "Have the panel handle the editor output and emit its own named component output for the application shell to bind."
        },
        {
          "id": "choice-5ffa9d85e5",
          "label": "Have the panel listen on document and forward a matching native event to the shell component."
        }
      ],
      "prompt": "A `<profile-editor>` inside `<settings-panel>` emits `saveRequested`. The application shell binds `(saveRequested)` on `<settings-panel>`, but its handler does not run. The editor must remain nested, and the shell should receive one explicit component event. Which change fixes the boundary?",
      "revision": 1,
      "technology": "angular"
    },
    {
      "competency": "dependency-provider-scope",
      "difficultyBand": "core",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-angular-provider-scope-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-696f260d7d",
          "label": "Register `PreferencesService` in each component's providers so their component injectors share one instance."
        },
        {
          "id": "choice-3b6eaeabca",
          "label": "Register `PreferencesService` in both routes' providers so their route injectors share one instance."
        },
        {
          "id": "choice-7adefd2662",
          "label": "Declare `PreferencesService` with `providedIn: 'root'` so both components resolve one root-injector instance."
        }
      ],
      "prompt": "Two unrelated dashboard components under different routes inject `PreferencesService` and must receive the same service instance for the application's lifetime. Which provider placement directly matches that requirement?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "code": {
        "language": "typescript",
        "runtime": "angular-17",
        "source": "form = new FormGroup({\n  email: new FormControl('')\n});\n\nsubmit() {\n  if (this.form.invalid) return;\n  this.api.createAccount(this.form.value).subscribe();\n}"
      },
      "competency": "reactive-form-validation-boundary",
      "difficultyBand": "stretch",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-angular-reactive-form-guard-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-8572038f39",
          "label": "Pair `Validators.required` with `Validators.email` on that control."
        },
        {
          "id": "choice-7fff8ac45f",
          "label": "Pair `Validators.required` with `Validators.minLength(1)` on that control."
        },
        {
          "id": "choice-49fb315741",
          "label": "Pair `Validators.email` with `Validators.maxLength(254)` on that control."
        }
      ],
      "prompt": "A reactive signup form already guards its submit method when the form is invalid. Which email-control configuration makes both an empty value and a malformed email invalid?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "competency": "ssr-platform-specific-dependency-boundary",
      "difficultyBand": "stretch",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-angular-ssr-platform-service-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-9c5fce67d6",
          "label": "Inject `PLATFORM_ID` into every analytics caller and guard each browser SDK invocation with `isPlatformBrowser`."
        },
        {
          "id": "choice-6e8d986843",
          "label": "Keep one analytics implementation for both platforms, import both dependencies there, and branch when the global `window` is unavailable."
        },
        {
          "id": "choice-4bf76fe5ef",
          "label": "Define an analytics token, provide its browser class normally, and override it with a server class in server configuration."
        }
      ],
      "prompt": "An Angular 22 checkout runs with SSR and hydration. Analytics must use a browser SDK in the browser and a server logger during SSR. Components must inject one API, and neither implementation may import the other platform's dependency. Which design satisfies that boundary?",
      "revision": 1,
      "technology": "angular"
    },
    {
      "code": {
        "language": "typescript",
        "runtime": "angular-17-to-22",
        "source": "results$ = this.query.valueChanges.pipe(\n  debounceTime(200),\n  distinctUntilChanged(),\n  mergeMap(query => this.http.get<Result[]>('/search', { params: { query } }))\n);"
      },
      "competency": "rxjs-latest-request-ownership",
      "difficultyBand": "core",
      "estimatedSeconds": 150,
      "format": "production-scenario",
      "id": "int-angular-switchmap-search-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-ae44f2ecf9",
          "label": "Use `concatMap` to queue each request and emit every response in query order."
        },
        {
          "id": "choice-5a0d863a93",
          "label": "Use `switchMap` to replace the active request and emit only the latest query's response."
        },
        {
          "id": "choice-143e30ad89",
          "label": "Use `exhaustMap` to keep the active request and ignore newer queries until it completes."
        }
      ],
      "prompt": "An Angular autocomplete maps `query.valueChanges` to `HttpClient.get`. With `mergeMap`, an older request can finish last and replace newer suggestions. The API supports cancellation through unsubscription. Which operator change best enforces latest-query ownership?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "competency": "rxjs-signal-lifecycle",
      "difficultyBand": "core",
      "estimatedSeconds": 90,
      "format": "production-scenario",
      "id": "int-angular-tosignal-lifetime-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-c9d5340ce1",
          "label": "Create `toSignal(updates$, { initialValue })` in each panel's injection context and retain automatic cleanup."
        },
        {
          "id": "choice-65d2d06373",
          "label": "Create `toSignal(updates$, { initialValue })` once in a root service and share that application-lived subscription."
        },
        {
          "id": "choice-888c8f5dcf",
          "label": "Create `toSignal(updates$, { initialValue, manualCleanup: true })` in each panel and omit explicit teardown."
        }
      ],
      "prompt": "An Angular 22 live-price panel converts a long-lived `updates$` stream to a signal. Each mounted panel must own one subscription, and destroyed panels must stop listening. Which setup gives the signal the required lifetime?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "code": {
        "language": "typescript",
        "runtime": "angular-22",
        "source": "@ViewChild('chartCanvas') canvas!: ElementRef<HTMLCanvasElement>;\nprivate chart?: Chart;"
      },
      "competency": "view-query-resource-lifecycle",
      "difficultyBand": "stretch",
      "estimatedSeconds": 150,
      "format": "production-scenario",
      "id": "int-angular-viewchild-chart-lifecycle-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-d21f72e1c6",
          "label": "Create the chart in the constructor with `document.querySelector`, then destroy it after the next navigation event."
        },
        {
          "id": "choice-57a9624db1",
          "label": "Read the canvas with `ViewChild`, create the chart in `ngAfterViewInit`, then destroy it in `ngOnDestroy`."
        },
        {
          "id": "choice-d10c67286e",
          "label": "Create the chart from `ngAfterViewChecked` when the canvas exists, replacing the previous chart on each check."
        }
      ],
      "prompt": "A client-only Angular 22 dashboard renders `<canvas #chartCanvas>` unconditionally. A chart library needs the canvas after the component view exists, creates one long-lived chart instance, and exposes `destroy()`. Construction in `ngOnInit` sees an unset query. Which lifecycle owns setup and teardown?",
      "revision": 1,
      "technology": "angular"
    },
    {
      "competency": "zoneless-onpush-view-notification",
      "difficultyBand": "core",
      "estimatedSeconds": 135,
      "format": "conceptual",
      "id": "int-angular-zoneless-view-notification-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-524f3fa9f3",
          "label": "Assign the field in the observer callback, then call `ChangeDetectorRef.markForCheck()` for that view."
        },
        {
          "id": "choice-ed30561541",
          "label": "Assign the field inside `NgZone.run()` and rely on leaving that callback to refresh the view."
        },
        {
          "id": "choice-a3da5f6a0d",
          "label": "Assign the field in the observer callback, then call `ApplicationRef.tick()` for the whole application."
        }
      ],
      "prompt": "An Angular 22 app uses zoneless change detection. A `ResizeObserver` adapter assigns a plain `width` field read by an OnPush template, but the text stays stale. The adapter contract must keep the plain field, and unrelated views must not be force-refreshed. Which notification is the narrowest correct integration?",
      "revision": 2,
      "technology": "angular"
    },
    {
      "competency": "content-box-size-calculation",
      "difficultyBand": "foundation",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-css-box-sizing-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-85a71956d5",
          "label": "`244px`; the width covers content, then 40px of horizontal padding and 4px of borders are added."
        },
        {
          "id": "choice-5bbcbe8518",
          "label": "`200px`; the declared width already includes content, padding, and borders under content-box."
        },
        {
          "id": "choice-338e1c235e",
          "label": "`240px`; horizontal padding is added to width, while borders do not affect the outer size."
        }
      ],
      "prompt": "A card has `width: 200px; padding: 20px; border: 2px solid; box-sizing: content-box`. Ignoring margins, what is its rendered outer width and why?",
      "revision": 1,
      "technology": "css"
    },
    {
      "competency": "flex-item-minimum-size",
      "difficultyBand": "core",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-css-flex-min-width-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-ccbbb88d86",
          "label": "Set `.main { width: 100%; }` so the item claims the row width before the sidebar is reserved."
        },
        {
          "id": "choice-19f4008397",
          "label": "Set `.main { min-width: 0; }` so its used width may fall below the content-based minimum."
        },
        {
          "id": "choice-9fe27c3c50",
          "label": "Set `.main { flex-shrink: 0; }` so the item preserves the filename's intrinsic width during layout."
        }
      ],
      "prompt": "A flex row has a fixed-width sidebar and `.main { flex: 1; }`. One unbreakable filename makes `.main` wider than the remaining space. Which declaration directly allows that flex item to shrink below its automatic content minimum?",
      "revision": 3,
      "technology": "css"
    },
    {
      "competency": "intrinsic-ratio-layout-reservation",
      "difficultyBand": "foundation",
      "estimatedSeconds": 90,
      "format": "conceptual",
      "id": "int-css-image-intrinsic-ratio-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-33350f531f",
          "label": "Give every feed image the same estimated `min-height`, then remove that placeholder after its load event."
        },
        {
          "id": "choice-db5d42c2aa",
          "label": "Emit each asset's `width` and `height` attributes, then apply `max-width: 100%; height: auto` responsively."
        },
        {
          "id": "choice-02072ee3c0",
          "label": "Wait for each `load`, read its natural dimensions, and then assign the matching wrapper aspect ratio."
        }
      ],
      "prompt": "Field data shows layout shifts across a publishing platform's responsive feeds when CDN images load. Editors choose different ratios, every delivery variant carries intrinsic pixel metadata, and the shared image contract must reserve the right box before resource selection without fixing rendered size. Which platform change meets those constraints?",
      "revision": 3,
      "technology": "css"
    },
    {
      "competency": "responsive-grid-track-sizing",
      "difficultyBand": "stretch",
      "estimatedSeconds": 150,
      "format": "production-scenario",
      "id": "int-css-responsive-grid-overflow-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-a0413333b1",
          "label": "Define auto-fit tracks with `minmax(min(100%, 18rem), 1fr)` and allow each URL to wrap anywhere."
        },
        {
          "id": "choice-d91fdd92e4",
          "label": "Use `repeat(auto-fit, minmax(18rem, 1fr))` and give unbroken URLs emergency wrap points."
        },
        {
          "id": "choice-b511809ff1",
          "label": "Build an auto-fit grid around a bounded `12rem` floor and give unbroken URLs emergency wrap points."
        }
      ],
      "prompt": "A card grid should create as many columns as fit, prefer cards at least `18rem` wide, collapse safely below that size, and keep a long URL fully readable without horizontal page overflow. Which rule set satisfies the full contract?",
      "revision": 2,
      "technology": "css"
    },
    {
      "competency": "stacking-context-boundaries",
      "difficultyBand": "core",
      "estimatedSeconds": 90,
      "format": "conceptual",
      "id": "int-css-stacking-context-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-62e747ced2",
          "label": "The modal z-index is still too small; increase it until it exceeds every value used by the header."
        },
        {
          "id": "choice-ed5ecd0fa2",
          "label": "Keep the transformed card and add `isolation: isolate` to the modal so its z-index can outrank the header."
        },
        {
          "id": "choice-d1beaac95b",
          "label": "The transformed card creates a stacking context; move the modal outside it or change the ancestor stack."
        }
      ],
      "prompt": "A modal has `position: fixed; z-index: 999999`, but it is rendered inside a card with `transform: translateZ(0)`. A sibling page header with `z-index: 10` still paints above the modal. Which explanation and fix address the root cause?",
      "revision": 2,
      "technology": "css"
    },
    {
      "competency": "adaptive-visual-effects-and-containment",
      "difficultyBand": "stretch",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-css-user-preferences-containment-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-c2c926f974",
          "label": "Use both media queries with text status and system borders, then apply `contain: paint` to the interactive card."
        },
        {
          "id": "choice-41ec89d26e",
          "label": "Stop the animation and add text, keep the boundary as a `box-shadow`, and isolate paint on the shimmer layer."
        },
        {
          "id": "choice-0ca72f9a1e",
          "label": "Stop the animation, expose text status, use system-color borders, and isolate paint on the decorative shimmer."
        }
      ],
      "prompt": "A loading card uses an infinite shimmer; its loading state and boundary are only visual. In reduced-motion mode the shimmer must stop, and in forced-colors mode both state and boundary must remain perceivable. Profiling suggests paint containment for the decorative shimmer layer, but focus indicators may extend beyond the card. Which policy meets the contract?",
      "revision": 2,
      "technology": "css"
    },
    {
      "competency": "script-loading-order",
      "difficultyBand": "foundation",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-html-defer-order-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-6073cc6798",
          "label": "Add `defer` to the inline bootstrap tag and leave it between the two deferred external scripts."
        },
        {
          "id": "choice-e53bf6a500",
          "label": "Move `startApp()` into `bootstrap.js`, then defer config, app, and bootstrap in dependency order."
        },
        {
          "id": "choice-c78a2ebaa7",
          "label": "Make all three scripts external and `async`, keeping dependency order only in the markup."
        }
      ],
      "prompt": "A page places deferred `config.js`, then inline `startApp()`, then deferred `app.js` in `<head>`. In production `startApp()` runs before its dependencies. All three steps must execute `config` → `app` → `startApp` after parsing without blocking it. What is the smallest correct change?",
      "revision": 4,
      "technology": "html"
    },
    {
      "competency": "modal-dialog-focus-contract",
      "difficultyBand": "core",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-html-dialog-focus-contract-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-a2e9b450ec",
          "label": "Queue through one coordinator, use `showModal()`, set initial focus, then restore a connected opener or the closed dialog."
        },
        {
          "id": "choice-13bc78c911",
          "label": "Queue through one coordinator, use `show()`, set `aria-modal`, set initial focus, then restore the opener or page heading."
        },
        {
          "id": "choice-c827d82460",
          "label": "Queue through one coordinator, use `showModal()`, set initial focus, then restore a connected opener or page heading."
        }
      ],
      "prompt": "A design-system modal accepts requests from several portal-mounted features. An audit finds competing dialogs, background controls in the Tab order, and route changes that can detach an opener before close. If detached, focus must move to a stable heading in the new route. The browser supports native `<dialog>`. Which policy enforces the contract?",
      "revision": 4,
      "technology": "html"
    },
    {
      "competency": "stateful-icon-control-semantics",
      "difficultyBand": "core",
      "estimatedSeconds": 75,
      "format": "conceptual",
      "id": "int-html-icon-button-name-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-13e7ae0432",
          "label": "Use a native button, bind `disabled` while pending, derive its `aria-label` from confirmed state, and hide the SVG."
        },
        {
          "id": "choice-2166e15b1c",
          "label": "Use a native button, set only `aria-disabled` while pending, derive its label from optimistic state, and hide the SVG."
        },
        {
          "id": "choice-1a33c34241",
          "label": "Use a button-role span, set `aria-disabled` while pending, derive its label from confirmed state, and hide the SVG."
        }
      ],
      "prompt": "An icon-only audio control sends an asynchronous mute request. While pending it must prevent repeat activation; after confirmation its accessible name must state the next action, and the SVG must stay decorative. Which implementation aligns interaction, confirmed state, and naming?",
      "revision": 3,
      "technology": "html"
    },
    {
      "competency": "explicit-form-label-association",
      "difficultyBand": "core",
      "estimatedSeconds": 60,
      "format": "production-scenario",
      "id": "int-html-label-submit-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-730c9ae1ed",
          "label": "Move `Email` into the input's `placeholder` and remove the separate visible span from the form."
        },
        {
          "id": "choice-5812ee0366",
          "label": "Keep the visible span beside the input and add `aria-label=\"Email\"` directly to the input."
        },
        {
          "id": "choice-6d6e29c2cc",
          "label": "Replace the span with `<label for=\"email\">Email</label>` and keep the matching input `id`."
        }
      ],
      "prompt": "A form renders `<span>Email</span><input id=\"email\" name=\"email\">`. The visible Email text must provide the input's accessible name, and clicking that text must focus the field. Which markup change meets both requirements?",
      "revision": 2,
      "technology": "html"
    },
    {
      "competency": "native-button-semantics",
      "difficultyBand": "foundation",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-html-native-button-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-194b7f3f57",
          "label": "Keep the `div`, add `tabindex=\"0\"`, and emulate both Enter and Space activation in a key handler."
        },
        {
          "id": "choice-7282c9b141",
          "label": "Use `<button type=\"button\">Place order</button>` and attach the same in-page action handler."
        },
        {
          "id": "choice-4ef72844e2",
          "label": "Use `<a href=\"#\">Place order</a>` and cancel navigation before running the same action handler."
        }
      ],
      "prompt": "A checkout drawer uses `<div role=\"button\" onclick=\"placeOrder()\">Place order</div>`. Mouse activation works, but Tab skips it and Enter and Space do nothing. It performs a non-submit in-page action, and no custom element is required. Which fix needs the least custom interaction code?",
      "revision": 3,
      "technology": "html"
    },
    {
      "competency": "valid-interactive-content-for-ssr",
      "difficultyBand": "foundation",
      "estimatedSeconds": 90,
      "format": "conceptual",
      "id": "int-html-valid-navigation-ssr-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-dbdb162d4b",
          "label": "Keep a single native link for the destination and apply the required card styling to that link."
        },
        {
          "id": "choice-b7a26611b2",
          "label": "Keep both anchors, call `preventDefault()` on the outer click handler, and suppress that subtree's hydration warning."
        },
        {
          "id": "choice-e2f9ce0521",
          "label": "Remove the outer `href`, add `role=\"presentation\"`, and keep the receipt anchor nested inside it."
        }
      ],
      "prompt": "A server-rendered card wraps a receipt-link component in `<a href=\"/receipt\">`, while the component renders another `<a href=\"/receipt\">View receipt</a>`. The parsed DOM contains sibling anchors before hydration, and the client reports a mismatch. The user action is navigation. What is the semantic root fix?",
      "revision": 1,
      "technology": "html"
    },
    {
      "competency": "abortable-request-ownership",
      "difficultyBand": "stretch",
      "estimatedSeconds": 150,
      "format": "production-scenario",
      "id": "int-js-abort-ownership-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-00d2a7af26",
          "label": "Share one controller across widgets; cancel it on any query, gate callbacks by a global run token, and silence `AbortError`."
        },
        {
          "id": "choice-0f12eaa15e",
          "label": "Store a controller per widget; cancel its predecessor, gate every settlement by that run's identity, and silence `AbortError`."
        },
        {
          "id": "choice-a5e40234a1",
          "label": "Store a controller per widget; cancel its predecessor, gate result and error writes, but let every run's `finally` clear loading."
        }
      ],
      "prompt": "Two autocomplete widgets can be mounted together, and each accepts overlapping queries through an endpoint that honors `AbortSignal`. A new query must cancel only that widget's prior request; stale settlements must not write results, loading, or errors; expected cancellation stays silent. Which ownership policy spans both instance and request lifetimes?",
      "revision": 3,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "async function load() {\n  try {\n    return Promise.reject('offline');\n  } catch {\n    console.log('inner-catch');\n    return 'cached';\n  } finally {\n    console.log('inner-finally');\n  }\n}\n\nload()\n  .catch((error) => {\n    console.log(`outer:${error}`);\n    return 'queued';\n  })\n  .finally(() => console.log('outer-finally'))\n  .then((value) => console.log(`value:${value}`));"
      },
      "competency": "async-rejection-propagation",
      "difficultyBand": "stretch",
      "estimatedSeconds": 150,
      "format": "code-output",
      "id": "int-js-async-return-rejection-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-e43a22f15b",
          "label": "inner-catch → inner-finally → outer-finally → value:cached"
        },
        {
          "id": "choice-826c3fab45",
          "label": "inner-finally → outer:offline → outer-finally → value:queued"
        },
        {
          "id": "choice-84060db0f6",
          "label": "inner-finally → outer-finally → outer:offline → value:queued"
        }
      ],
      "prompt": "What does this code log in a standards-compliant browser?",
      "revision": 2,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "if (button.dataset.enabled) {\n  runAction();\n}"
      },
      "competency": "dom-dataset-string-coercion",
      "difficultyBand": "stretch",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-js-dataset-boolean-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-f857e17860",
          "label": "Compare `button.dataset.enabled === 'true'` before running the action."
        },
        {
          "id": "choice-576324eda5",
          "label": "Wrap `button.dataset.enabled` with `Boolean()` before running the action."
        },
        {
          "id": "choice-4a8ab5ea5e",
          "label": "Use `button.dataset.enabled ?? false` before running the action."
        }
      ],
      "prompt": "Server-rendered buttons carry `data-enabled=\"true\"` or `data-enabled=\"false\"`. The current handler still runs for disabled records because it tests `if (button.dataset.enabled)`. The HTML attribute contract cannot change. Which check makes the handler honor both values?",
      "revision": 2,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "const list = document.createElement('div');\nconst button = document.createElement('button');\nlist.append(button);\n\nlist.addEventListener('click', (event) => {\n  console.log(event.target.tagName);\n  console.log(event.currentTarget.tagName);\n});\n\nbutton.click();"
      },
      "competency": "event-target-and-listener-owner",
      "difficultyBand": "core",
      "estimatedSeconds": 90,
      "format": "code-output",
      "id": "int-js-event-target-currenttarget-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-a72cdba8d3",
          "label": "DIV → DIV"
        },
        {
          "id": "choice-7857d2ac10",
          "label": "BUTTON → BUTTON"
        },
        {
          "id": "choice-30484588aa",
          "label": "BUTTON → DIV"
        }
      ],
      "prompt": "What does the delegated click handler log, in order, in a standards-compliant browser?",
      "revision": 1,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "Promise.reject('network')\n  .catch((error) => {\n    console.log(`caught:${error}`);\n    return 'cached';\n  })\n  .finally(() => console.log('cleanup'))\n  .then((value) => console.log(`value:${value}`));"
      },
      "competency": "promise-finally-transparency",
      "difficultyBand": "core",
      "estimatedSeconds": 120,
      "format": "code-output",
      "id": "int-js-finally-recovery-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-3274b7aaa9",
          "label": "caught:network → cleanup → value:cached"
        },
        {
          "id": "choice-d551c98aed",
          "label": "cleanup → caught:network → value:cached"
        },
        {
          "id": "choice-bdfd4a8be4",
          "label": "caught:network → cleanup → value:undefined"
        }
      ],
      "prompt": "What does this Promise chain log, in order, in a standards-compliant browser?",
      "revision": 4,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "Promise.resolve('start')\n  .then((value) => {\n    console.log(value);\n    throw new Error('boom');\n  })\n  .finally(() => console.log('finally-1'))\n  .catch((error) => {\n    console.log(error.message);\n    return 'recovered';\n  })\n  .finally(() => {\n    console.log('finally-2');\n    throw new Error('cleanup');\n  })\n  .then(\n    (value) => console.log(value),\n    (error) => console.log(error.message),\n  );"
      },
      "competency": "promise-recovery-and-finally-failure",
      "difficultyBand": "stretch",
      "estimatedSeconds": 180,
      "format": "code-output",
      "id": "int-js-finally-throws-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-0e2fc15e16",
          "label": "start → finally-1 → boom → finally-2 → recovered"
        },
        {
          "id": "choice-312fd4b338",
          "label": "start → boom → finally-1 → finally-2 → cleanup"
        },
        {
          "id": "choice-7b0b744ee5",
          "label": "start → finally-1 → boom → finally-2 → cleanup"
        }
      ],
      "prompt": "What does this Promise chain log, in order, in a standards-compliant browser?",
      "revision": 1,
      "technology": "javascript"
    },
    {
      "competency": "event-listener-identity-cleanup",
      "difficultyBand": "foundation",
      "estimatedSeconds": 90,
      "format": "conceptual",
      "id": "int-js-listener-cleanup-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-a69e33471f",
          "label": "Store one `onResize` function, add it with capture true, then remove that function with capture false."
        },
        {
          "id": "choice-56f121d348",
          "label": "Keep capture true in both calls, but create a fresh `onResize` arrow when the panel unmounts."
        },
        {
          "id": "choice-e4a78142c6",
          "label": "Store one `onResize` function and pass it with capture true to both the add and remove calls."
        }
      ],
      "prompt": "A panel registers a resize listener with an inline arrow and `{ capture: true }`, then removes a new inline arrow without specifying capture. Repeated mounts leave old callbacks active. Which cleanup fixes both matching boundaries without removing other components' listeners?",
      "revision": 3,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "console.log('open');\n\nsetTimeout(() => {\n  console.log('notify');\n  queueMicrotask(() => console.log('after-notify'));\n}, 0);\n\nsetTimeout(() => console.log('archive'), 0);\n\nPromise.resolve().then(() => {\n  console.log('validate');\n  queueMicrotask(() => console.log('commit'));\n});\n\nqueueMicrotask(() => console.log('measure'));\nconsole.log('close');"
      },
      "competency": "nested-microtask-fifo-order",
      "difficultyBand": "stretch",
      "estimatedSeconds": 120,
      "format": "code-output",
      "id": "int-js-nested-microtask-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-4aa027b68f",
          "label": "open → close → validate → commit → measure → notify → after-notify → archive"
        },
        {
          "id": "choice-d36c528b59",
          "label": "open → close → validate → measure → commit → notify → archive → after-notify"
        },
        {
          "id": "choice-8f266a175e",
          "label": "open → close → validate → measure → commit → notify → after-notify → archive"
        }
      ],
      "prompt": "What does this code log, in order, in a standards-compliant browser?",
      "revision": 4,
      "technology": "javascript"
    },
    {
      "competency": "nullish-default-handling",
      "difficultyBand": "foundation",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-js-nullish-default-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-34d6545231",
          "label": "Use `const pageSize = settings.pageSize ?? 20;` so only nullish values select the default."
        },
        {
          "id": "choice-33f9bf8ac4",
          "label": "Use `const pageSize = settings.pageSize || 20;` so every falsy value selects the default."
        },
        {
          "id": "choice-14cd09d3d9",
          "label": "Use `const pageSize = settings.pageSize === null ? 20 : settings.pageSize;` to default only `null`."
        }
      ],
      "prompt": "An API may return `0`, `\"\"`, `false`, `null`, or `undefined` for `settings.pageSize`. Preserve every explicit falsy value and use `20` only when the value is `null` or `undefined`. Which assignment meets that contract?",
      "revision": 1,
      "technology": "javascript"
    },
    {
      "competency": "observer-resource-lifecycle",
      "difficultyBand": "core",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-js-observer-lifecycle-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-aee99617f4",
          "label": "Keep one global observer forever and let every mounted list add sentinels without removing them."
        },
        {
          "id": "choice-6e87bf32ad",
          "label": "Own one observer per mount, unobserve removed sentinels, and disconnect it when that list unmounts."
        },
        {
          "id": "choice-8aab4d7334",
          "label": "Set the local observer variable to `null` on unmount and rely on garbage collection for its targets."
        }
      ],
      "prompt": "A routed infinite list creates a new `IntersectionObserver` on every mount. After several visits, detached sentinels remain observed and old callbacks still request pages. Which lifecycle policy fixes the retained work without sharing route state globally?",
      "revision": 1,
      "technology": "javascript"
    },
    {
      "code": {
        "language": "javascript",
        "runtime": "browser",
        "source": "console.log('render');\n\nsetTimeout(() => console.log('timer'), 0);\n\nPromise.resolve().then(() => console.log('promise'));\n\nconsole.log('commit');"
      },
      "competency": "task-and-microtask-order",
      "difficultyBand": "core",
      "estimatedSeconds": 90,
      "format": "code-output",
      "id": "int-js-task-order-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-d74b9e1325",
          "label": "render → commit → timer → promise"
        },
        {
          "id": "choice-7ea0dfa862",
          "label": "render → commit → promise → timer"
        },
        {
          "id": "choice-46db452f77",
          "label": "render → promise → timer → commit"
        }
      ],
      "prompt": "What does this code log, in order, in a standards-compliant browser?",
      "revision": 1,
      "technology": "javascript"
    },
    {
      "competency": "bounded-worker-generation-ownership",
      "difficultyBand": "foundation",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-js-worker-long-task-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-d08885cf72",
          "label": "Use a bounded shared Worker pool; coalesce queued jobs per widget, tag widget generations, and gate main-thread commits per widget."
        },
        {
          "id": "choice-565ae3f79b",
          "label": "Use a bounded shared Worker pool; coalesce queued jobs globally, tag one shared generation, and gate main-thread commits globally."
        },
        {
          "id": "choice-1790b8f8e7",
          "label": "Start one Worker per payload; tag jobs by widget generation, gate main-thread commits per widget, and terminate Workers after settlement."
        }
      ],
      "prompt": "Eight independently mounted dashboard widgets receive payloads faster than their pure 300 ms transforms finish. The design must bound total worker concurrency, coalesce superseded queued work per widget, prevent one widget from invalidating another, and let only each widget's latest compact result update the DOM. Which architecture meets the full contract?",
      "revision": 3,
      "technology": "javascript"
    },
    {
      "competency": "controlled-input-edit-normalization",
      "difficultyBand": "stretch",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-react-controlled-input-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-1b1d13d138",
          "label": "Store `Number(event.target.value)` on every change; stringify it for display, and let Reset replace that number."
        },
        {
          "id": "choice-383ae59733",
          "label": "Render committed numbers with `defaultValue`; leave interim text in the DOM, and save its parsed value on blur."
        },
        {
          "id": "choice-ad5701d9d0",
          "label": "Store editing text as controlled string state; parse on blur, and let Reset replace that string."
        }
      ],
      "prompt": "A React price field must let a parent Reset replace its displayed text immediately. During editing, users may temporarily enter `''`, `'-'`, or `'12.'`; that exact text must remain visible until blur, when valid input is normalized and committed as a number. Which state design preserves both contracts?",
      "revision": 3,
      "technology": "react"
    },
    {
      "competency": "derived-state",
      "difficultyBand": "foundation",
      "estimatedSeconds": 75,
      "format": "conceptual",
      "id": "int-react-derived-filter-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-4fced9e93d",
          "label": "Derive `visibleProducts` in the parent on every render and pass it to a child wrapped in `memo`."
        },
        {
          "id": "choice-5dd53e2369",
          "label": "Derive `visibleProducts` in a `useMemo` calculation with `products` and `query` as dependencies."
        },
        {
          "id": "choice-8ab9362e52",
          "label": "Derive `visibleProducts` from `products` and a `useDeferredValue(query)` during every render."
        }
      ],
      "prompt": "A Profiler trace shows that filtering a large `products` array is expensive. Its identity stays stable while unchanged, but unrelated state still rerenders the component. The list is a pure function of `products` and `query`. React may reuse a render-time cache while dependencies match, but correctness must survive cache eviction. Which refactor removes Effect-copied state and can avoid repeating the filter on unrelated renders?",
      "revision": 3,
      "technology": "react"
    },
    {
      "competency": "effect-resource-lifecycle",
      "difficultyBand": "core",
      "estimatedSeconds": 105,
      "format": "conceptual",
      "id": "int-react-effect-connection-cleanup-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-5afd94a8df",
          "label": "Disconnect the ref's previous connection at each setup, store the replacement, and return no cleanup from the Effect."
        },
        {
          "id": "choice-74ff89e73a",
          "label": "Replace a connection ref at each setup, then use a separate mount-only cleanup to disconnect its current value on unmount."
        },
        {
          "id": "choice-cd66552e7f",
          "label": "Create the connection per Effect run and return cleanup that disconnects that run before replacement or unmount."
        }
      ],
      "prompt": "A chat component opens a new server connection in an Effect keyed by `roomId`. A room change must replace the connection, unmount must close it, and development Strict Mode probes setup and cleanup. Which ownership contract covers every boundary?",
      "revision": 3,
      "technology": "react"
    },
    {
      "competency": "subscription-stale-closure-updates",
      "difficultyBand": "core",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-react-functional-queue-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-97db5d8904",
          "label": "Subscribe for `socket`, add each delta with `setTotal(previous => previous + delta)`, and return the listener cleanup."
        },
        {
          "id": "choice-8d0a206f25",
          "label": "Subscribe with `[socket, total]`, add each delta with `setTotal(total + delta)`, and clean up before each resubscription."
        },
        {
          "id": "choice-3a2d2342fe",
          "label": "Subscribe once for `socket`, copy `total` inside the handler, add the delta, and pass that local value to `setTotal`."
        }
      ],
      "prompt": "A React total subscribes once to a stable `socket` in an Effect. A burst can deliver several deltas before the next render. Its handler calls `setTotal(total + delta)`, so updates overwrite one another; the subscription must not restart whenever `total` changes. Which implementation preserves every delta and the listener lifecycle?",
      "revision": 1,
      "technology": "react"
    },
    {
      "competency": "immutable-object-state-update",
      "difficultyBand": "core",
      "estimatedSeconds": 60,
      "format": "conceptual",
      "id": "int-react-immutable-object-state-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-bdf526cfa1",
          "label": "Assign `profile.name = nextName`, then call `setProfile(profile)` with the existing object reference."
        },
        {
          "id": "choice-ee11643ebe",
          "label": "Call `setProfile({ ...profile, name: nextName })` to provide a new object with the updated field."
        },
        {
          "id": "choice-b0f1acaebe",
          "label": "Store the new name in `useRef`, mutate `profile`, and read the ref during the next component render."
        }
      ],
      "prompt": "A React component stores `profile` in state. Its rename handler mutates `profile.name` and passes the same object back to `setProfile`, so the screen may stay stale. Which update follows React's state model?",
      "revision": 1,
      "technology": "react"
    },
    {
      "competency": "shared-state-ownership",
      "difficultyBand": "core",
      "estimatedSeconds": 120,
      "format": "conceptual",
      "id": "int-react-lift-shared-state-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-b1bc8a85be",
          "label": "Have each panel derive `isOpen` from `activePanelId` and report clicks to the parent that owns the identifier."
        },
        {
          "id": "choice-25a60efcb5",
          "label": "Have each panel own local `isOpen` and update that value only from the panel's own click handler."
        },
        {
          "id": "choice-6eb79b8deb",
          "label": "Have each panel own local `isOpen` and notify its sibling directly whenever one panel is activated."
        }
      ],
      "prompt": "Two accordion panels receive `activePanelId` from their nearest parent. Each also initializes local `isOpen` from that prop. After the parent resets the identifier to `null`, a panel can remain open. No component outside this parent and its panels needs the value. Which design gives Reset and panel clicks one consistent state owner?",
      "revision": 8,
      "technology": "react"
    },
    {
      "competency": "profile-guided-render-memoization",
      "difficultyBand": "core",
      "estimatedSeconds": 150,
      "format": "conceptual",
      "id": "int-react-profile-stable-props-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-c75af119fc",
          "label": "Memoize every surface with a comparator that checks `data` only, while parents keep rebuilding equivalent `options`."
        },
        {
          "id": "choice-bd23a4969b",
          "label": "Memoize each surface and freeze its initial `options` object in the parent with an empty dependency list."
        },
        {
          "id": "choice-86b1c7e2ce",
          "label": "Memoize each surface and stabilize parent-owned `options` with every value that can change their contents as dependencies."
        }
      ],
      "prompt": "Across several high-frequency dashboards, large virtualized tables and charts use a shared `{ data, options }` prop API. Profiler traces show unrelated ticker updates rerendering each expensive surface because parents rebuild equivalent options; those options must still change when filters or theme change. The surfaces read no changing context, React Compiler is disabled, and behavior must stay unchanged. Which cross-team policy creates safe prop-based bailouts?",
      "revision": 4,
      "technology": "react"
    },
    {
      "competency": "context-update-boundaries",
      "difficultyBand": "stretch",
      "estimatedSeconds": 135,
      "format": "production-scenario",
      "id": "int-react-split-hot-context-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-7a2b3874ec",
          "label": "Memoize one combined `{ theme, pointer }` value with both fields in the provider's dependencies."
        },
        {
          "id": "choice-1018f6ace1",
          "label": "Provide `theme` and `pointer` through separate route contexts and subscribe each consumer to its field."
        },
        {
          "id": "choice-e488bfa860",
          "label": "Wrap `ThemePreview` in `memo` while keeping its subscription to the combined route context."
        }
      ],
      "prompt": "Within one route, `RouteContext` serves a `ThemePreview` that reads only `theme` and a `PointerReadout` that reads only coordinates updated on every move. Profiling shows the preview rerendering for coordinate changes. Context must remain the distribution mechanism. Which local refactor isolates the hot updates?",
      "revision": 2,
      "technology": "react"
    },
    {
      "competency": "ssr-multiroot-id-coordination",
      "difficultyBand": "core",
      "estimatedSeconds": 165,
      "format": "production-scenario",
      "id": "int-react-ssr-multiroot-useid-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-171f18ff58",
          "label": "Give each root a distinct `identifierPrefix`, and reuse that root's prefix in its server render and `hydrateRoot` call."
        },
        {
          "id": "choice-c2ef6e19de",
          "label": "Give both roots one shared `identifierPrefix`, then append a random browser suffix to every `useId` result."
        },
        {
          "id": "choice-25a300dc45",
          "label": "Omit `identifierPrefix` on the server, then assign separate prefixes only in the two browser `hydrateRoot` calls."
        }
      ],
      "prompt": "One document contains two independently server-rendered React 19 roots. Both call `useId` for label/input pairs. Within each root, server markup must hydrate unchanged, and IDs must not collide across roots. Which configuration satisfies both constraints?",
      "revision": 1,
      "technology": "react"
    },
    {
      "competency": "list-key-reconciliation-boundary",
      "difficultyBand": "core",
      "estimatedSeconds": 120,
      "format": "conceptual",
      "id": "int-react-stable-list-key-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-d3c93d66f6",
          "label": "Move `product.id` to each mapped `<EditableRow>`; remove the redundant key from its nested input."
        },
        {
          "id": "choice-07566a4635",
          "label": "Add the current `index` key to each mapped `<EditableRow>` and keep `product.id` on its nested input."
        },
        {
          "id": "choice-082c02dcf6",
          "label": "Leave each row unkeyed and reset its draft from `product.name` in an Effect whenever `product.id` changes."
        }
      ],
      "prompt": "`EditableRow` is returned directly by `products.map` and owns an unsaved draft. Its nested input has `key={product.id}`, but rows may reorder. Which identity contract keeps the draft with its record?",
      "revision": 1,
      "technology": "react"
    },
    {
      "code": {
        "language": "jsx",
        "runtime": "react-19.2",
        "source": "useEffect(() => {\n  setStatus('loading');\n\n  fetchSearch(query)\n    .then((items) => setResults(items))\n    .catch((error) => setError(error))\n    .finally(() => setStatus('idle'));\n}, [query]);"
      },
      "competency": "effect-async-race-ownership",
      "difficultyBand": "core",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-react-stale-search-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-681c0cc8fd",
          "label": "Use one module-level generation counter; each run records it and commits only while its recorded value remains current."
        },
        {
          "id": "choice-23ee524b51",
          "label": "Use one component-level mounted ref; only unmount cleanup clears it and every asynchronous handler checks the ref."
        },
        {
          "id": "choice-ad1b440775",
          "label": "Use one Effect-run invalidation flag; that run's cleanup sets it and every asynchronous handler checks the flag."
        }
      ],
      "prompt": "Several routes can mount independent instances of this autocomplete. Within one instance, query changes can start overlapping requests, and unmount can occur before settlement. `fetchSearch` is a legacy client with no cancellation support. Every result, error, and loading write must come from that same instance's current Effect run, without invalidating another instance's current run. Which change enforces this?",
      "revision": 3,
      "technology": "react"
    },
    {
      "competency": "concurrent-update-priorities",
      "difficultyBand": "core",
      "estimatedSeconds": 165,
      "format": "production-scenario",
      "id": "int-react-transition-input-priority-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-abb1c54418",
          "label": "Place both `setQuery(next)` and `setFilterQuery(next)` inside one `startTransition` callback."
        },
        {
          "id": "choice-211f2d6325",
          "label": "Run `setQuery(next)` normally, then place `setFilterQuery(next)` inside `startTransition`."
        },
        {
          "id": "choice-2035bfeb0d",
          "label": "Force `setQuery(next)` with `flushSync`, then run `setFilterQuery(next)` synchronously afterward."
        }
      ],
      "prompt": "A controlled search input updates `query`, and the same handler updates `filterQuery` used by a costly result tree. Keystrokes feel blocked. The input must reflect every key immediately, while result rendering may be interrupted. Which scheduling change matches those priorities?",
      "revision": 1,
      "technology": "react"
    },
    {
      "competency": "composable-effect-lifetime",
      "difficultyBand": "core",
      "estimatedSeconds": 120,
      "format": "conceptual",
      "id": "int-vue-composable-lifecycle-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-ef6510bde2",
          "label": "Add the listener in `onMounted`, remove that same listener in `onUnmounted`, and return the reactive width ref."
        },
        {
          "id": "choice-50f4f35ab0",
          "label": "Add the listener when the composable module loads, keep it for the page lifetime, and return the width ref."
        },
        {
          "id": "choice-bbe2ea9afe",
          "label": "Start a timer in the composable, register both lifecycle hooks inside its callback, and return the width ref."
        }
      ],
      "prompt": "A component calls `useViewportWidth()` synchronously from `<script setup>`. The composable must expose reactive width and leave no `resize` listener after its component unmounts. Which lifetime design best satisfies both requirements?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "derived-state-and-reactive-side-effects",
      "difficultyBand": "foundation",
      "estimatedSeconds": 90,
      "format": "conceptual",
      "id": "int-vue-computed-watch-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-6269fe34da",
          "label": "Mirror `grandTotal` into a writable ref with a default price watcher, then watch `currency` post-flush for analytics."
        },
        {
          "id": "choice-1fe784316c",
          "label": "Derive `grandTotal` with a computed ref from prices, then watch `currency` post-flush for analytics."
        },
        {
          "id": "choice-e0f9dd610e",
          "label": "Derive `grandTotal` with a computed ref from prices, then watch `currency` at default pre-flush timing."
        }
      ],
      "prompt": "A Vue 3.5 checkout renders `grandTotal`, which is defined entirely by reactive `subtotal` and `tax` and has no independent write path. Separately, after `currency` changes, analytics must inspect the summary after its component DOM patch. Which design assigns both responsibilities correctly?",
      "revision": 6,
      "technology": "vue"
    },
    {
      "competency": "component-v-model-contract",
      "difficultyBand": "stretch",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-vue-custom-input-model-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-18888ec083",
          "label": "Create the model ref with `defineModel({ required: true })` and bind the inner input's `v-model` to it."
        },
        {
          "id": "choice-d5691dd89f",
          "label": "Declare a `modelValue` prop and assign directly to that prop from the inner input's event handler."
        },
        {
          "id": "choice-f127f8bb37",
          "label": "Copy `modelValue` into a local ref once and emit a generic `change` event after the inner input blurs."
        }
      ],
      "prompt": "A Vue 3.5 `PriceInput` wraps a native input and is used as `<PriceInput v-model=\"price\" />`. The parent value must update on typing and later parent changes must appear in the input. Which child implementation best establishes that contract?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "keepalive-shared-resource-ownership",
      "difficultyBand": "core",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-vue-keepalive-active-socket-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-e594abd3fb",
          "label": "Let each pane open its channel socket in `onActivated`, close it in `onDeactivated`, and keep the handle local to that pane."
        },
        {
          "id": "choice-1391049086",
          "label": "Share a channel socket from a registry, but close and delete it whenever any pane's `onDeactivated` hook releases its listener."
        },
        {
          "id": "choice-02b3edbdb0",
          "label": "Use a channel-keyed registry: acquire and attach in `onActivated`, release in `onDeactivated`, and close/delete at zero subscribers."
        }
      ],
      "prompt": "Two independently mounted Vue 3.5 `<KeepAlive>` regions can display panes for the same channel at once. The backend permits one WebSocket per channel; every active pane needs messages, deactivated panes must detach, and the socket must close only after the last active subscriber releases it, including cache eviction. Which ownership policy satisfies the full contract?",
      "revision": 3,
      "technology": "vue"
    },
    {
      "code": {
        "language": "vue",
        "runtime": "vue-3.5",
        "source": "<template>\n  <span class=\"icon\" aria-hidden=\"true\">★</span>\n  <button type=\"button\"><slot /></button>\n</template>"
      },
      "competency": "multiroot-fallthrough-attribute-forwarding",
      "difficultyBand": "stretch",
      "estimatedSeconds": 120,
      "format": "production-scenario",
      "id": "int-vue-multiroot-fallthrough-attrs-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-da9e3af793",
          "label": "Bind `$attrs` to the decorative `<span>` so inherited classes and listeners have one explicit target."
        },
        {
          "id": "choice-0464995a9c",
          "label": "Wrap both roots in a `<div>` and rely on automatic fallthrough to place inherited classes and listeners."
        },
        {
          "id": "choice-20f05c005a",
          "label": "Set `inheritAttrs: false` and bind `$attrs` to the `<button>` so classes and listeners reach the control."
        }
      ],
      "prompt": "A Vue 3.5 `<ActionButton>` renders a decorative `<span>` and a native `<button>` as two root nodes. A parent passes `class` and `@click`; both must reach the native button without declaring every possible attribute or listener as a prop. Which component policy establishes that target?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "ssr-store-request-ownership",
      "difficultyBand": "core",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-vue-pinia-request-scope-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-08cb186562",
          "label": "Reset the shared Pinia state at the start of each request, then render concurrently with that singleton."
        },
        {
          "id": "choice-654b0f9457",
          "label": "Create the Vue app and a new `createPinia()` container per request, then serialize only its store state."
        },
        {
          "id": "choice-f6793e14cb",
          "label": "Reuse the shared Pinia but create fresh component instances per request before serializing its store state."
        }
      ],
      "prompt": "A Node server creates one Pinia instance at module scope and reuses it for every Vue 3.5 SSR request. Under concurrent traffic, one user's cart can appear in another response. Which architecture best fixes the ownership boundary?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "component-state-ownership",
      "difficultyBand": "core",
      "estimatedSeconds": 75,
      "format": "conceptual",
      "id": "int-vue-prop-event-ownership-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-4b11c85646",
          "label": "Emit a quantity-change event with the next quantity value, then let the parent update the cart line it owns."
        },
        {
          "id": "choice-55b8024762",
          "label": "Mutate the cart line object's quantity inside the child, then let that nested prop change flow to the parent."
        },
        {
          "id": "choice-cf40c32b71",
          "label": "Copy the prop's quantity into a child ref once, then edit that ref without notifying the parent component."
        }
      ],
      "prompt": "A Vue 3.5 cart row receives a `line` object as a prop. The parent owns the cart and recalculates totals when quantities change. Without adding a shared store, how should the child request a new quantity?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "reactive-prop-watch-sources",
      "difficultyBand": "stretch",
      "estimatedSeconds": 150,
      "format": "production-scenario",
      "id": "int-vue-prop-watch-getter-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-9fc7fb66da",
          "label": "Create `ref(productId)` once and watch that local ref with `immediate: true` for product loads."
        },
        {
          "id": "choice-6460e4645f",
          "label": "Watch `() => productId` with `immediate: true` so Vue reads the reactive prop whenever the watcher runs."
        },
        {
          "id": "choice-3eb5d0b8ce",
          "label": "Place `productId` in a plain array and watch that array with `immediate: true` for every product load."
        }
      ],
      "prompt": "In Vue 3.5 `<script setup>`, a child destructures `productId` from `defineProps` and calls `watch(productId, loadProduct)`. Vue warns that a value, not a reactive source, was passed. Which replacement tracks later prop changes and also performs the initial load?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "composition-api-reactive-primitives",
      "difficultyBand": "foundation",
      "estimatedSeconds": 75,
      "format": "conceptual",
      "id": "int-vue-ref-primitive-jr-v1",
      "level": "junior",
      "options": [
        {
          "id": "choice-c4cecbe708",
          "label": "Use `reactive(0)` and assign the returned primitive directly whenever the counter changes in script code."
        },
        {
          "id": "choice-51ae9adf8a",
          "label": "Use `ref(0)` and write through `count.value` in script while the template directly reads `count` without `.value`."
        },
        {
          "id": "choice-a59579b58d",
          "label": "Use `computed(() => 0)` and overwrite the computed result directly whenever the counter changes in script code."
        }
      ],
      "prompt": "A Vue 3.5 Composition API component needs a numeric counter that event handlers replace and the template renders. Which declaration and access pattern correctly keeps the primitive reactive?",
      "revision": 2,
      "technology": "vue"
    },
    {
      "competency": "render-update-prop-stability",
      "difficultyBand": "core",
      "estimatedSeconds": 150,
      "format": "conceptual",
      "id": "int-vue-stable-row-props-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-a9f2114a27",
          "label": "Cache each row type with `<KeepAlive>` while the shared contract keeps allocating a fresh `viewConfig` object."
        },
        {
          "id": "choice-a1fb6894b7",
          "label": "Pass the changing `selectedId` through the shared contract and let every row compare it with its product ID."
        },
        {
          "id": "choice-6add07f66e",
          "label": "Have the shared contract pass a per-row `selected` Boolean and stable `currency` primitive as separate props."
        }
      ],
      "prompt": "Profiling a Vue 3.5 trading UI with several large virtualized product viewports shows that high-frequency `selectedId` changes update every mounted `ProductRow`. A shared row-prop adapter creates a new `viewConfig` object for each row containing `selected` and unchanged `currency`; every viewport uses that contract, so its fan-out consumes the render budget. Which contract redesign most directly narrows updates across the system?",
      "revision": 3,
      "technology": "vue"
    },
    {
      "competency": "ssr-hydration-determinism",
      "difficultyBand": "core",
      "estimatedSeconds": 180,
      "format": "production-scenario",
      "id": "int-vue-timezone-hydration-sr-v1",
      "level": "senior",
      "options": [
        {
          "id": "choice-da92204662",
          "label": "Render the same UTC text on the server and initial client pass, then replace it with local text in `onMounted`."
        },
        {
          "id": "choice-edf423c1ff",
          "label": "Render local text independently on both sides, then add `data-allow-mismatch` to silence the hydration warning."
        },
        {
          "id": "choice-586b4ecc61",
          "label": "Render UTC text on the server, then use `createApp` on the client to discard that DOM and mount local text."
        }
      ],
      "prompt": "A Vue 3.5 SSR page renders an event time. The server does not know the browser's time zone, so formatting locally during both renders produces different text and a hydration warning. SEO still requires meaningful server text. Which change best removes the mismatch?",
      "revision": 1,
      "technology": "vue"
    },
    {
      "competency": "watcher-request-cancellation",
      "difficultyBand": "core",
      "estimatedSeconds": 150,
      "format": "production-scenario",
      "id": "int-vue-watcher-abort-cleanup-mid-v1",
      "level": "mid",
      "options": [
        {
          "id": "choice-b47b1ca3c5",
          "label": "Keep one controller outside the watcher; abort and replace it at each run, but register no cleanup when the watcher stops."
        },
        {
          "id": "choice-f73aa5aee8",
          "label": "Create a controller per run, ignore stale results with a request token, and abort only after the response settles."
        },
        {
          "id": "choice-344f9df631",
          "label": "Create a controller per run, register its abort synchronously with `onWatcherCleanup`, then fetch with its signal."
        }
      ],
      "prompt": "A component-owned Vue 3.5 watcher fetches suggestions whenever `query` changes. `fetch` accepts an `AbortSignal`; an obsolete request must stop when a new run begins, and the current request must stop when the watcher ends. Which implementation owns both boundaries?",
      "revision": 2,
      "technology": "vue"
    }
  ],
  "language": "en",
  "schemaVersion": "2.0.0",
  "status": "editorial-gold"
}
