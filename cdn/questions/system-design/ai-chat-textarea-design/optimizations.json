{
  "key": "O",
  "title": "Optimizations",
  "blocks": [
    {
      "type": "steps",
      "title": "Scenario: a Stop response is lost",
      "steps": [
        {
          "title": "Keep the composer responsive",
          "text": "The assistant reply is streaming from streamId s1. Send is rendered as Stop, yet the user may edit a separate draft. Recent history is paged independently, so neither a long transcript nor current-turn recovery blocks typing."
        },
        {
          "title": "Store the Stop intent",
          "text": "The user activates Stop. The coordinator creates stopCommandId x1, targets s1, and may abort the local reader for immediate feedback. The partial assistant text remains visible with a stopping status."
        },
        {
          "title": "Recover uncertain authority",
          "text": "The Stop response is lost. Retrying reuses x1; it does not invent another cancellation intent. Local abort still is not authoritative Stop, so the client reconnects with afterSequence or asks getTurnSnapshot for s1."
        },
        {
          "title": "Reject the late result",
          "text": "A delayed delta or Send acknowledgement from s1 is accepted only while its command, message, stream, and sequence match the active turn. Once a terminal snapshot is stored, any later s1 event is ignored without erasing preserved text."
        },
        {
          "title": "Create a new retry turn",
          "text": "After stopped or failed is authoritative, Retry or Regenerate creates a new commandId and receives streamId s2. A late Stop result for x1 may confirm old history, but it cannot change s2 or the new draft."
        }
      ]
    },
    {
      "type": "heading",
      "text": "Accessibility behavior"
    },
    {
      "type": "text",
      "text": "Give the textarea a persistent label and describe Enter versus modified Enter without overriding platform composition. Each attachment exposes its name, phase, error, Retry, and Remove controls. Do not announce token deltas; use one concise polite announcement when Send becomes Stop and when the turn completes, stops, or fails. Keep focus in the composer after ordinary Send, and move it only for a recovery action the user requested."
    },
    {
      "type": "heading",
      "text": "Draft confidentiality and safe rendering"
    },
    {
      "type": "text",
      "text": "Namespace persisted drafts and attachment handles by accountId plus conversationId. On account switch, detach the previous account's in-memory draft before loading the next namespace. On logout, delete drafts and local handles unless a product-approved retention policy applies; retained drafts need a bounded TTL and a visible clear-drafts control. Browser storage is not a confidentiality boundary, so draft text must not enter telemetry, and sensitive modes may use memory-only drafts. Cross-tab coordination elects one current-turn owner while letting each tab keep transient composition state."
    },
    {
      "type": "text",
      "text": "Treat assistant content as untrusted even when it arrives from the product API. Parse only supported Markdown, sanitize the result, allowlist link protocols, and render code as inert text; never pass streamed or completed assistant HTML directly to innerHTML. Apply that boundary to partial updates because unfinished Markdown is still untrusted input."
    },
    {
      "type": "heading",
      "text": "Performance and measurement"
    },
    {
      "type": "text",
      "text": "Page older history independently from current-turn state. Add list virtualization or coalesce visual delta commits only after profiling representative long conversations and low-end devices; accepted sequence state still updates correctly even when paints are batched. Test IME composition, duplicate Enter, attachment races, lost Send and Stop responses, reconnect gaps, account changes, hostile Markdown, keyboard recovery, and narrow layouts. Compare duplicate-message rate, stale-event suppression, stop latency, draft loss, input responsiveness, and recovery completion by device and network cohort."
    },
    {
      "type": "image",
      "src": "questions/system-design/ai-chat-textarea-design/chat-stop-retry-timeline.svg",
      "alt": "Timeline where stop and retry replace the active stream and a late event from the old stream is ignored.",
      "caption": "Stop and retry timeline: stable command identities converge while stale stream events cannot mutate the new turn.",
      "width": 960,
      "height": 420,
      "fallbackText": "Text fallback: Stop targets stream one with a stable stop command. Retry creates stream two after reconciliation. A delayed delta from stream one fails the active-stream check and is ignored."
    },
    {
      "type": "links",
      "title": "Technical references",
      "items": [
        {
          "label": "MDN composition events",
          "href": "https://developer.mozilla.org/en-US/docs/Web/API/Element/compositionstart_event",
          "description": "IME composition lifecycle for text controls."
        },
        {
          "label": "MDN AbortController",
          "href": "https://developer.mozilla.org/en-US/docs/Web/API/AbortController",
          "description": "Cancellation primitive for request and stream work."
        }
      ],
      "editorialRole": "references"
    }
  ]
}
