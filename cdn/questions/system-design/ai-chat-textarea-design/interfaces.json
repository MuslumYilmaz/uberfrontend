{
  "key": "I",
  "title": "Interface",
  "blocks": [
    {
      "type": "text",
      "text": "Expose draft editing independently from the current response. The view receives attachment readiness, a primary action that is Send or Stop, the only active turn, and terminal Retry or Regenerate. Raw transport parsing and history loading remain outside view components."
    },
    {
      "type": "code",
      "language": "ts",
      "code": "interface SendCommand {\n  commandId: string;\n  clientMessageId: string;\n  text: string;\n  attachmentIds: string[];\n}\n\ninterface StopCommand {\n  stopCommandId: string;\n  conversationId: string;\n  streamId: string;\n}\n\ntype TurnEvent =\n  | { type: 'message.delta'; conversationId: string; streamId: string; messageId: string; sequence: number; delta: string }\n  | { type: 'message.completed'; conversationId: string; streamId: string; messageId: string; sequence: number }\n  | { type: 'message.stopped'; conversationId: string; streamId: string; messageId: string; sequence: number }\n  | { type: 'message.failed'; conversationId: string; streamId: string; messageId: string; sequence: number; errorCode: string };\n\ninterface OpenTurnEventsInput {\n  conversationId: string;\n  streamId: string;\n  afterSequence: number;\n  signal: AbortSignal;\n}\n\ninterface UseChatComposerResult {\n  draft: ComposerDraft;\n  interaction: ComposerInteractionState;\n  attachments: AttachmentDraft[];\n  activeTurn: StreamMessage | null;\n  primaryAction: 'send' | 'stop';\n  updateText(text: string): void;\n  send(command: SendCommand): Promise<void>;\n  retransmitSend(): Promise<void>;\n  stop(command: StopCommand): Promise<void>;\n  retryAfterTerminal(command: SendCommand): Promise<void>;\n}\n\ninterface ChatTurnClient {\n  send(input: SendCommand & { conversationId: string; signal: AbortSignal }): Promise<{\n    acceptedCommandId: string;\n    acceptedClientMessageId: string;\n    messageId: string;\n    assistantMessageId: string;\n    streamId: string;\n  }>;\n  openEvents(input: OpenTurnEventsInput): AsyncIterable<TurnEvent>;\n  stop(input: StopCommand & { signal: AbortSignal }): Promise<{\n    acceptedStopCommandId: string;\n  }>;\n  getTurnSnapshot(input: {\n    conversationId: string;\n    commandId: string;\n    clientMessageId: string;\n    streamId: string;\n    signal: AbortSignal;\n  }): Promise<TurnSnapshot>;\n}",
      "validation": {
        "kind": "contract",
        "level": "typecheck",
        "group": "ai-chat-textarea-design-contract"
      }
    },
    {
      "type": "code",
      "language": "text",
      "code": "event: message.delta\ndata: {\"conversationId\":\"c_123\",\"streamId\":\"s_123\",\"messageId\":\"m_123\",\"sequence\":1,\"delta\":\"hello\"}\n\nevent: message.completed\ndata: {\"conversationId\":\"c_123\",\"streamId\":\"s_123\",\"messageId\":\"m_123\",\"sequence\":2}\n\n",
      "validation": {
        "kind": "protocol",
        "protocol": "sse",
        "dataFormat": "json"
      }
    },
    {
      "type": "steps",
      "title": "Integration flow",
      "steps": [
        {
          "title": "Restore",
          "text": "Restore the account-and-conversation draft independently from recent messages."
        },
        {
          "title": "Send or retransmit",
          "text": "After composition ends and attachments are ready, call send with stable command and client message IDs. If only its response is lost, retransmit that same commandId."
        },
        {
          "title": "Open or resume events",
          "text": "Open with afterSequence equal to lastSequence. Accept a next-sequence event only for the active conversationId, streamId, and messageId."
        },
        {
          "title": "Stop and reconcile",
          "text": "Create one stopCommandId per user intent and reuse it after a lost response. Local reading may abort, but resumed events or getTurnSnapshot must still confirm authoritative terminal status."
        },
        {
          "title": "Retry after terminal",
          "text": "Only complete, stopped, or failed exposes Retry or Regenerate. That user action creates a new Send command and accepts a new streamId; the old command is never repurposed."
        }
      ]
    },
    {
      "type": "heading",
      "text": "UI-facing contract"
    },
    {
      "type": "text",
      "text": "Send accepts text, finalized attachment IDs, commandId, clientMessageId, and AbortSignal at the client boundary. AbortSignal ending a browser request says nothing about whether the server accepted Send or Stop. The coordinator therefore retains the original command, resumes ordered events, or requests an authoritative snapshot. Components see a single primary action and terminal recovery; they never parse SSE or choose which late event wins."
    }
  ]
}
