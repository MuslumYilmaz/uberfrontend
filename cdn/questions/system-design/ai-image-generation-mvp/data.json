{
  "key": "D",
  "title": "Data",
  "blocks": [
    {
      "type": "text",
      "text": "Model the generation job as a versioned asynchronous resource. Progress is optional and can be determinate or indeterminate; it is never fabricated from elapsed time. Keep pending create, retry, and cancel commands separate from server-confirmed job state."
    },
    {
      "type": "divider"
    },
    {
      "type": "columns",
      "columns": [
        {
          "blocks": [
            {
              "type": "text",
              "text": "Model each generation as a Job with status, prompt, and output URLs. The UI renders a list of jobs and shows the active one."
            }
          ]
        },
        {
          "blocks": [
            {
              "type": "text",
              "text": "State-model checks:\n- Clear job status field.\n- Output URL stored separately from prompt.\n- Metadata to render history.\n- Error fields for failures."
            }
          ]
        }
      ]
    },
    {
      "type": "divider"
    },
    {
      "type": "table",
      "title": "Core entities",
      "columns": [
        "Entity",
        "What it represents",
        "Why it matters"
      ],
      "rows": [
        [
          "GenerationJob",
          "A single prompt-to-image request",
          "Primary UI unit"
        ],
        [
          "JobStatus",
          "queued/running/cancelling/cancelled/done/failed",
          "Drives loading and error UI"
        ],
        [
          "Asset",
          "Image URL + metadata",
          "Displayed in the results grid"
        ]
      ]
    },
    {
      "type": "code",
      "language": "typescript",
      "code": "type JobStatus = 'queued' | 'running' | 'cancelling' | 'cancelled' | 'done' | 'failed';\ntype JobProgress =\n  | { kind: 'indeterminate' }\n  | { kind: 'determinate'; value: number };\n\ninterface GenerationJob {\n  id: string;\n  statusUrl: string;\n  prompt: string;\n  status: JobStatus;\n  version: number;\n  progress?: JobProgress;\n  image?: { url: string; width: number; height: number };\n  error?: { code: string; message: string; retryable: boolean };\n  createdAt: number;\n}\n\ninterface PendingCommand {\n  idempotencyKey: string;\n  kind: 'create' | 'retry' | 'cancel';\n  promptSnapshot?: string;\n  jobId?: string;\n  basedOnVersion?: number;\n}\n\ninterface GenerationState {\n  jobsById: Record<string, GenerationJob>;\n  activeJobId?: string;\n  pendingByKey: Record<string, PendingCommand>;\n}\n",
      "validation": {
        "kind": "contract",
        "level": "typecheck",
        "group": "ai-image-generation-mvp-contract"
      }
    },
    {
      "type": "columns",
      "columns": [
        {
          "blocks": [
            {
              "type": "checklist",
              "title": "Explicit state",
              "items": [
                "Job status and timestamps.",
                "Output URL for the generated image.",
                "Error field for failed jobs.",
                "A list of recent jobs for history."
              ]
            }
          ]
        },
        {
          "blocks": [
            {
              "type": "checklist",
              "title": "Generation-state pitfalls",
              "items": [
                "No status field, so UI cannot show progress.",
                "No job id, so polling is impossible.",
                "Embedding base64 images instead of URLs.",
                "No history list, so users lose past results."
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "stats",
      "items": [
        {
          "label": "Primary entity",
          "value": "GenerationJob",
          "helperText": "Everything revolves around the job lifecycle."
        },
        {
          "label": "Output",
          "value": "imageUrl",
          "helperText": "Serve via CDN for speed."
        },
        {
          "label": "History",
          "value": "jobs[]",
          "helperText": "Small list of recent items."
        }
      ]
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "State checkpoint",
      "text": "The MVP model remains compact but precise: versioned status, honest progress semantics, safe output metadata, retryable error, and separate pending command intent."
    },
    {
      "type": "heading",
      "text": "Command, job, and media ownership"
    },
    {
      "type": "text",
      "text": "Store prompt snapshots separately from versioned jobs and pending commands. A create command is keyed before any job ID exists and links to the returned statusUrl later. Progress is a tagged union of determinate and indeterminate. Every authoritative transition has a unique monotonic version; equal-version contradictions trigger refetch rather than client precedence."
    },
    {
      "type": "table",
      "title": "Client model",
      "columns": [
        "Record",
        "Key fields",
        "Owner"
      ],
      "rows": [
        [
          "PromptDraft",
          "text, options, safety acknowledgement",
          "Local state"
        ],
        [
          "GenerationJob",
          "id, status, version, progress, output",
          "Server cache"
        ],
        [
          "PendingCommand",
          "key, kind, basedOnVersion",
          "Action state"
        ],
        [
          "JobMonitor",
          "cursor, poll delay, freshness",
          "Runtime"
        ]
      ]
    }
  ]
}
