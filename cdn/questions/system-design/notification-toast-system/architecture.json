{
  "key": "A",
  "title": "Architecture",
  "blocks": [
    {
      "type": "table",
      "title": "Ownership map",
      "columns": [
        "Owner",
        "Keeps",
        "Does not own"
      ],
      "rows": [
        [
          "Toast store",
          "Normalized records, visible order, overflow order, and terminal removal state.",
          "Browser timeout handles or DOM nodes."
        ],
        [
          "Lifecycle coordinator",
          "One expiry handle per timed record and idempotent disposal by ID.",
          "Visual stacking, focus, or live-region text."
        ],
        [
          "Toast viewport",
          "A portal view of at most three records plus keyboard-operable actions.",
          "Independent timers, copied records, or announcement history."
        ],
        [
          "Announcer",
          "A stable live region and a set of announcement IDs already emitted.",
          "Toast lifetime or visual placement."
        ]
      ]
    },
    {
      "type": "callout",
      "variant": "success",
      "title": "Canonical model",
      "text": "toast.success(...) creates a command. The store assigns an ID and places its record in the visible stack or ordered queue. The lifecycle coordinator schedules expiry only when the normalized lifetime is timed. The viewport renders store state and returns user intent. The announcer receives an eligible announcement event once; it does not infer new speech from every render.",
      "editorialRole": "canonical-model"
    },
    {
      "type": "steps",
      "title": "Profile-save flow",
      "steps": [
        {
          "title": "Accept the command",
          "text": "A feature or service calls toast.success('Profile saved', { dedupeKey: 'profile-save:42' }). The API validates the options and sends an add command to the store."
        },
        {
          "title": "Admit the record",
          "text": "The store assigns a stable ID. If fewer than three records are visible it appends the record there; otherwise it appends the distinct record to the ordered queue."
        },
        {
          "title": "Schedule and render",
          "text": "The lifecycle coordinator schedules the expiry for this timed status. The viewport renders the same record through the root portal, while the announcer publishes Profile saved only if that ID has not been announced."
        },
        {
          "title": "Converge removal",
          "text": "Dismiss and timeout submit the same ID. The store accepts the first transition, the coordinator clears the handle, and a queued record may become visible. Any late signal is a no-op."
        }
      ]
    },
    {
      "type": "table",
      "title": "Worked example: profile save and edit conflict",
      "columns": [
        "Event",
        "State change",
        "User-visible result"
      ],
      "rows": [
        [
          "Profile save succeeds",
          "Insert one polite, timed status record.",
          "Profile saved appears without moving focus."
        ],
        [
          "The same success event repeats",
          "Match its safe dedupe key instead of inserting another record.",
          "The sentence is not duplicated visually or audibly."
        ],
        [
          "An edit conflict arrives",
          "Insert a persistent action record with a Review command.",
          "Review remains available until resolution or safe dismissal."
        ],
        [
          "Dismiss races with expiry",
          "Only the first remove transition changes store state; cleanup runs once.",
          "The toast disappears once and the next queued record is stable."
        ]
      ]
    },
    {
      "type": "image",
      "src": "questions/system-design/notification-toast-system/toast-command-lifecycle.svg",
      "alt": "Flow from a global toast command through the store, lifecycle coordinator, viewport, and one-time live-region announcer.",
      "caption": "Command and lifecycle ownership: rendering never owns timers or announcement identity.",
      "width": 960,
      "height": 540,
      "fallbackText": "Text fallback: A global command enters the toast store. The lifecycle coordinator schedules or cancels expiry, the viewport renders at most three records, and the announcer speaks each eligible toast ID once."
    }
  ]
}
