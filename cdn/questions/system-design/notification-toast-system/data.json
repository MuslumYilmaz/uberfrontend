{
  "key": "D",
  "title": "Data",
  "blocks": [
    {
      "type": "text",
      "text": "Keep store records separate from browser runtime and announcement history. The store can then reconcile commands without serializing timeout handles or tying speech to a rendered row."
    },
    {
      "type": "code",
      "language": "typescript",
      "code": "type ToastVariant = 'success' | 'error' | 'warning' | 'info';\ntype ToastPlacement = 'top-start' | 'top-end' | 'bottom-start' | 'bottom-end';\ntype ToastLifetime =\n  | { kind: 'persistent' }\n  | { kind: 'timed'; durationMs: number };\ntype ToastActionState = 'idle' | 'pending' | 'failed';\n\ninterface ToastRecord {\n  id: string;\n  dedupeKey?: string;\n  variant: ToastVariant;\n  message: string;\n  description?: string;\n  placement: ToastPlacement;\n  lifetime: ToastLifetime;\n  urgency: 'polite' | 'urgent';\n  action?: {\n    label: string;\n    commandId: string;\n    state: ToastActionState;\n  };\n}\n\ninterface ToastRuntime {\n  toastId: string;\n  deadline?: number;\n  timerHandle?: ReturnType<typeof setTimeout>;\n  status: 'scheduled' | 'removing' | 'disposed';\n}\n\ninterface AnnouncementState {\n  emittedIds: Set<string>;\n}\n\ninterface ToastState {\n  visible: ToastRecord[];\n  queued: ToastRecord[];\n  defaultPlacement: ToastPlacement;\n  defaultTimedDurationMs: number;\n  maxVisible: number;\n}",
      "validation": {
        "kind": "contract",
        "level": "typecheck",
        "group": "notification-toast-system-contract"
      }
    },
    {
      "type": "steps",
      "title": "State transitions",
      "steps": [
        {
          "title": "Add",
          "text": "Normalize the command into a ToastRecord with a unique ID. Put it in visible when capacity remains; otherwise append the distinct record to queued. A safe dedupe key may update an existing matching record rather than create a copy."
        },
        {
          "title": "Schedule",
          "text": "For a timed lifetime, the lifecycle coordinator creates one ToastRuntime keyed by toastId. Persistent records have no expiry handle, so Review or Undo cannot disappear because a provider default was applied accidentally."
        },
        {
          "title": "Remove",
          "text": "The first dismiss or timeout marks runtime removing, clears its handle, removes the record, and promotes the next queued item. A later request sees removing, disposed, or a missing ID and returns without another state change."
        },
        {
          "title": "Announce",
          "text": "The announcer checks emittedIds before updating its live region. It records identity outside ToastRecord, so a visual reorder, responsive move, or component rerender cannot make the same message sound new."
        }
      ]
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "Identity serves different jobs",
      "text": "id identifies one toast lifecycle, dedupeKey joins repeated reports of the same outcome, commandId identifies an action, and emittedIds prevents repeat speech. Reusing one field for all four jobs makes race behavior ambiguous."
    },
    {
      "type": "table",
      "title": "State ownership",
      "columns": [
        "State",
        "Owner",
        "Reason"
      ],
      "rows": [
        [
          "visible and queued",
          "Toast store",
          "One ordered source drives admission and promotion."
        ],
        [
          "ToastRuntime by toastId",
          "Lifecycle coordinator",
          "Browser handles are created and disposed in one place."
        ],
        [
          "Rendered items",
          "Toast viewport",
          "The UI derives rows from store records instead of copying them."
        ],
        [
          "emittedIds",
          "Announcer",
          "Announcement identity remains independent of the DOM."
        ]
      ]
    }
  ]
}
