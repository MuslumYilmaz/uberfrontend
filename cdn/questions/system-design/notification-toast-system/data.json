{
  "key": "D",
  "title": "Data",
  "blocks": [
    {
      "type": "text",
      "text": "Model durable toast content separately from lifecycle runtime so the store can reconcile update, action, dismissal, and teardown without serializing browser handles."
    },
    {
      "type": "divider"
    },
    {
      "type": "columns",
      "columns": [
        {
          "blocks": [
            {
              "type": "text",
              "text": "Use a ToastRecord for serializable content, ToastState for visible and queued records, and ToastRuntime for deadlines and pause reasons. Provider configuration supplies placement, timing, and overflow policy without becoming part of each caller's command."
            }
          ]
        },
        {
          "blocks": [
            {
              "type": "text",
              "text": "State-model checks:\n- Toast identity, variant, placement, lifetime, urgency, and action are explicit.\n- Persistent and timed lifetimes are mutually exclusive.\n- Runtime deadline and pause reasons stay outside the durable record.\n- Action state distinguishes idle, pending, and failed recovery."
            }
          ]
        }
      ]
    },
    {
      "type": "divider"
    },
    {
      "type": "code",
      "language": "typescript",
      "code": "type ToastVariant = 'success' | 'error' | 'warning' | 'info';\ntype ToastPlacement = 'top-start' | 'top-end' | 'bottom-start' | 'bottom-end';\ntype ToastLifetime =\n  | { kind: 'persistent' }\n  | { kind: 'timed'; durationMs: number };\ntype ToastActionState = 'idle' | 'pending' | 'failed';\n\ninterface ToastRecord {\n  id: string;\n  dedupeKey?: string;\n  variant: ToastVariant;\n  message: string;\n  description?: string;\n  placement: ToastPlacement;\n  lifetime: ToastLifetime;\n  urgency: 'polite' | 'urgent';\n  announced: boolean;\n  action?: {\n    label: string;\n    commandId: string;\n    state: ToastActionState;\n  };\n}\n\ninterface ToastRuntime {\n  remainingMs?: number;\n  deadline?: number;\n  timerHandle?: ReturnType<typeof setTimeout>;\n  pausedBy: Set<'focus' | 'hover' | 'page-hidden' | 'preference'>;\n}\n\ninterface ToastState {\n  visible: ToastRecord[];\n  queued: ToastRecord[];\n  defaultPlacement: ToastPlacement;\n  defaultTimedDurationMs: number;\n  maxVisible: number;\n}",
      "validation": {
        "kind": "contract",
        "level": "typecheck",
        "group": "notification-toast-system-contract"
      }
    },
    {
      "type": "table",
      "title": "Core entities",
      "columns": [
        "Entity",
        "Fields (example)",
        "Design rationale"
      ],
      "rows": [
        [
          "ToastRecord",
          "id, variant, message, placement, lifetime, urgency, action?",
          "Each toast explicitly models persistence or timing, one announcement state, and a command identity for an optional action."
        ],
        [
          "ToastState",
          "toasts[], defaultPlacement, defaultDurationMs, maxVisible",
          "The global slice keeps bounded visible records, queued records, and resolved provider defaults."
        ],
        [
          "ToastRuntime",
          "remainingMs, deadline, timerHandle, pausedBy",
          "The lifecycle coordinator owns runtime handles and pause accounting outside serializable store data."
        ]
      ]
    },
    {
      "type": "columns",
      "columns": [
        {
          "blocks": [
            {
              "type": "checklist",
              "title": "Required model fields",
              "items": [
                "A unique id per toast so you can remove it reliably.",
                "A variant field for visual style and icon (success/error/etc.).",
                "A clear message and optional description.",
                "A resolved placement field to control where it appears.",
                "A persistent or timed lifetime; actionable and critical items resolve to persistent.",
                "A status role by default and an alert role only for genuine urgency.",
                "An optional action object for interactive toasts."
              ]
            }
          ]
        },
        {
          "blocks": [
            {
              "type": "checklist",
              "title": "Keep outside the data model",
              "items": [
                "Raw DOM nodes or refs inside the toast object.",
                "Actual timer/timeout handles in the model (keep them alongside the component logic or in a separate map).",
                "Random UI-only flags that can be derived (e.g. computing \"isOld\" from createdAt instead of storing another boolean).",
                "Mixing unrelated global UI state (like modals) into ToastState.",
                "Overcomplicating the schema for a simple notification use case."
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "steps",
      "title": "How toast data typically changes over time",
      "steps": [
        {
          "title": "1. Add toast",
          "text": "A caller triggers toast.success(\"Saved\"). The API creates a ToastRecord with a unique ID and resolved semantic options, then inserts it into the visible stack or queue according to provider policy."
        },
        {
          "title": "2. Start lifetime",
          "text": "A lifecycle coordinator starts a countdown only for a timed status. It tracks remaining time and pause reasons outside the durable toast record so focus, hover, page visibility, and user preference can pause without creating conflicting timers."
        },
        {
          "title": "3. Manual or auto dismiss",
          "text": "A safe manual dismissal or an exhausted timed lifetime emits one remove command. Cleanup clears runtime handles before the toast leaves state; persistent actionable and critical messages do not disappear because a timer fired."
        },
        {
          "title": "4. Optional action",
          "text": "Invoking an action moves it to pending. Success dismisses or updates it according to policy; failure keeps the toast persistent, exposes retry, and restores focus to meaningful recovery text."
        }
      ]
    },
    {
      "type": "stats",
      "items": [
        {
          "label": "Must-have entities",
          "value": "ToastRecord + runtime",
          "helperText": "If you clearly define these two, the rest of the design becomes much easier to explain."
        },
        {
          "label": "Key idea",
          "value": "Model behavior as data",
          "helperText": "Variant, placement, lifetime, urgency, and announcement state are explicit data."
        },
        {
          "label": "Good signal",
          "value": "Small, readable types",
          "helperText": "If another engineer can understand your model from a short interface snippet, you’re on the right track."
        }
      ]
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "State checkpoint",
      "text": "A solid toast data model treats each toast as a small, self-contained piece of state with clear fields for variant, placement, lifetime, and accessibility. This keeps the rendering logic simple and makes the global store easy to reason about."
    },
    {
      "type": "heading",
      "text": "Toast state ownership"
    },
    {
      "type": "text",
      "text": "Keep durable toast content separate from runtime timer handles. Toast state contains message, variant, urgency, lifetime, dedupe key, announcement state, and optional action command. Runtime state contains deadline and pause reasons. This separation prevents serialization of browser handles and makes pause, update, dismissal, and teardown deterministic."
    },
    {
      "type": "table",
      "title": "Client model",
      "columns": [
        "Record",
        "Key fields",
        "Owner"
      ],
      "rows": [
        [
          "Toast",
          "content, urgency, lifetime, action",
          "Global UI store"
        ],
        [
          "ToastRuntime",
          "deadline, pause reasons, timer handle",
          "Lifecycle coordinator"
        ],
        [
          "OverflowPolicy",
          "maxVisible, queue, collapse rules",
          "Provider config"
        ],
        [
          "AnnouncementState",
          "announced, urgency, group key",
          "Accessibility coordinator"
        ]
      ]
    }
  ]
}
