{
  "key": "I",
  "title": "Interface",
  "blocks": [
    {
      "type": "text",
      "text": "Define a small, predictable API: props to configure rules and labels, events to notify the parent about changes, and accessible markup that doesn’t require consumers to manually wire low-level ARIA attributes."
    },
    {
      "type": "divider"
    },
    {
      "type": "columns",
      "columns": [
        {
          "blocks": [
            {
              "type": "text",
              "text": "Expose one ImageUploadPreview surface with validation rules, labels, an UploadClient, and callbacks. Consumers never manage object URLs or cancellation resources. onChange represents only the durable UploadedAsset or null; transient phases use onStatusChange and typed failures use onError."
            }
          ]
        },
        {
          "blocks": [
            {
              "type": "text",
              "text": "Contract checks:\n- A simple, high-level API that hides file handling complexity.\n- Configurable limits and labels via props instead of hard-coded values.\n- One change callback for the finalized asset or removal, plus optional error reporting.\n- Accessible by default: labels, aria-*, error messages handled internally.\n- Easy to extend to drag-and-drop or multiple files without breaking the contract."
            }
          ]
        }
      ]
    },
    {
      "type": "divider"
    },
    {
      "type": "code",
      "language": "ts",
      "code": "export interface UploadClient {\n  createSession(input: {\n    fileName: string;\n    mimeType: string;\n    size: number;\n    signal: AbortSignal;\n  }): Promise<UploadSession>;\n  transfer(input: {\n    session: UploadSession;\n    body: Blob;\n    signal: AbortSignal;\n    onProgress: (loaded: number, total?: number) => void;\n  }): Promise<void>;\n  finalize(input: {\n    sessionId: string;\n    idempotencyKey: string;\n    signal: AbortSignal;\n  }): Promise<UploadedAsset>;\n}\n\nexport interface ImageUploadPreviewProps {\n  value?: UploadedAsset | null;\n  uploadClient: UploadClient;\n  maxSizeBytes: number;\n  allowedMimeTypes: string[];\n  label: string;\n  description?: string;\n  inputId?: string;\n  describedBy?: string;\n  enableDropzone?: boolean;\n  disabled?: boolean;\n  onChange: (asset: UploadedAsset | null) => void;\n  onStatusChange?: (status: UploadStatus) => void;\n  onError?: (error: UploadError) => void;\n}\n",
      "validation": {
        "kind": "contract",
        "level": "typecheck",
        "group": "image-upload-preview-contract"
      }
    },
    {
      "type": "table",
      "title": "Core API surfaces",
      "columns": [
        "Surface",
        "Example shape",
        "How you explain it"
      ],
      "rows": [
        [
          "Configuration props",
          "maxSizeBytes, allowedMimeTypes, preprocessing policy",
          "These props configure product rules; UploadClient owns session creation, real transfer progress, and finalization."
        ],
        [
          "Value & change",
          "value, onChange",
          "The parent controls only the finalized asset. Local File, preview URL, progress, and session state remain component-owned."
        ],
        [
          "Status callbacks",
          "onStatusChange, onError",
          "Lifecycle and typed error callbacks expose transient UI state without pretending it is the form value."
        ],
        [
          "Accessibility hooks",
          "inputId, aria-describedby",
          "\"These hooks let the component integrate into bigger forms: external labels, shared helper text, or error messages from a form library.\""
        ],
        [
          "UX options",
          "enableDropzone, disabled",
          "\"Optional flags for nicer UX (drag-and-drop) and form states (disabling input during overall form submission).\""
        ]
      ]
    },
    {
      "type": "columns",
      "columns": [
        {
          "blocks": [
            {
              "type": "checklist",
              "title": "How the component behaves (from the outside)",
              "items": [
                "Selection creates a local attempt; onChange is not called until finalization returns an UploadedAsset.",
                "While a real transfer is running, status is uploading and progress reflects transferred bytes when the transport exposes them.",
                "Only server finalization produces an UploadedAsset and calls onChange with a durable value.",
                "If validation fails, status becomes error and onError receives a typed UploadError.",
                "Remove aborts the active attempt, revokes its object URL, resets local state, and calls onChange(null)."
              ]
            }
          ]
        },
        {
          "blocks": [
            {
              "type": "checklist",
              "title": "Accessibility behavior",
              "items": [
                "The control has a visible label (from label or external <label for>).",
                "Errors are rendered in an element with role=\"alert\" or wired via aria-describedby so screen readers announce them.",
                "\"Choose image\", \"Replace\", and \"Remove\" buttons are all keyboard-focusable and operable with Enter/Space.",
                "The preview image has an appropriate alt (e.g. \"Selected image preview\") or a prop to customize it.",
                "Focus order stays logical when the component appears in a form: label → main control → additional actions → error text."
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "steps",
      "title": "Typical usage from a parent component",
      "steps": [
        {
          "title": "1. Configure rules",
          "text": "Parent passes size/type limits and label text: <ImageUploadPreview maxSizeBytes={2 * 1024 * 1024} allowedMimeTypes={[\"image/jpeg\", \"image/png\"]} label=\"Upload avatar\" />."
        },
        {
          "title": "2. Listen for changes",
          "text": "Parent subscribes to onChange and stores only UploadedAsset or null in form state."
        },
        {
          "title": "3. React to upload lifecycle",
          "text": "Parent optionally uses onStatusChange and onError for page-level status, analytics, or submit availability."
        },
        {
          "title": "4. Integrate with form & a11y",
          "text": "Parent wires inputId and aria-describedby to integrate with larger form labels and helper/error text without breaking accessibility."
        },
        {
          "title": "5. Reset or re-use",
          "text": "On form reset, parent either clears its controlled value or relies on the component’s own \"Remove\" action to reset to the initial state."
        }
      ]
    },
    {
      "type": "stats",
      "items": [
        {
          "label": "Main prop groups",
          "value": "3",
          "helperText": "Config (rules), value (controlled/uncontrolled), events."
        },
        {
          "label": "Core event",
          "value": "onChange",
          "helperText": "Emits only a finalized UploadedAsset or null."
        },
        {
          "label": "Optional events",
          "value": "onStatusChange / onError",
          "helperText": "Used when the parent needs transient lifecycle details."
        }
      ]
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "Contract checkpoint",
      "text": "The public value is deliberately narrow: a finalized asset or null. File input quirks, object URLs, decoded validation, upload attempts, progress, cancellation, and ARIA wiring stay inside the component."
    },
    {
      "type": "heading",
      "text": "UI-facing contract"
    },
    {
      "type": "text",
      "text": "UploadClient creates a short-lived session, transfers a Blob with AbortSignal and progress callback when the transport supports bytes, then finalizes with an idempotency key. Progress can be indeterminate. A new selection invalidates the prior attempt before any callback can commit."
    },
    {
      "type": "steps",
      "title": "From local file to authoritative asset",
      "steps": [
        {
          "title": "Validate",
          "text": "Check declared type, file signature policy, size, and decoded dimensions before upload."
        },
        {
          "title": "Preview",
          "text": "Create one object URL, reserve geometry, and revoke it on replacement or teardown."
        },
        {
          "title": "Transfer",
          "text": "Use the active session with abort support and throttle only visual progress commits."
        },
        {
          "title": "Finalize",
          "text": "Convert the session into an asset and emit the durable parent value once."
        }
      ]
    }
  ]
}
