{
  "dependencies": {
    "react": "18.x",
    "react-dom": "18.x",
    "react-scripts": "5.0.1"
  },
  "openFile": "/src/App.tsx",
  "files": {
    "package.json": "{\n  \"name\": \"react-chessboard-click-highlight-solution\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"dependencies\": {\n    \"react\": \"18.x\",\n    \"react-dom\": \"18.x\",\n    \"react-scripts\": \"5.0.1\"\n  },\n  \"scripts\": { \"start\": \"react-scripts start\" }\n}\n",
    "public/index.html": "<!doctype html><html lang=\"en\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>React SDK • Chessboard Click Highlight (solution)</title></head><body><div id=\"root\"></div></body></html>",
    "src/index.tsx": "import React from 'react';\nimport { createRoot } from 'react-dom/client';\nimport App from './App';\n\ncreateRoot(document.getElementById('root')!).render(<App />);\n",
    "src/App.tsx": "import React, { useMemo, useState } from 'react';\nimport './App.css';\n\nconst MIN_SIZE = 2;\nconst MAX_SIZE = 20;\n\nfunction clampSize(value: number): number {\n  if (!Number.isFinite(value)) return MIN_SIZE;\n  return Math.max(MIN_SIZE, Math.min(MAX_SIZE, Math.trunc(value)));\n}\n\nfunction toKey(row: number, col: number): string {\n  return `${row}-${col}`;\n}\n\nexport default function App() {\n  const [size, setSize] = useState(8);\n  const [selectedKey, setSelectedKey] = useState<string | null>(null);\n\n  function onSizeChange(raw: string) {\n    const next = clampSize(Number(raw));\n    setSize(next);\n    setSelectedKey(null);\n  }\n\n  function onSelectCell(row: number, col: number) {\n    setSelectedKey(toKey(row, col));\n  }\n\n  const rows = useMemo(() => Array.from({ length: size }, (_, row) => row), [size]);\n  const cols = useMemo(() => Array.from({ length: size }, (_, col) => col), [size]);\n\n  const selectedLabel = selectedKey\n    ? (() => {\n        const [row, col] = selectedKey.split('-').map(Number);\n        return `Selected: row ${row + 1}, column ${col + 1}`;\n      })()\n    : 'Selected: none';\n\n  return (\n    <main className=\"page\">\n      <section className=\"card\">\n        <h1>Chessboard Click / Highlight</h1>\n        <p className=\"subtitle\">\n          Build an N×N chessboard where clicking one cell highlights it and clears previous selection.\n        </p>\n\n        <label className=\"size\" htmlFor=\"board-size\">\n          Board size (N)\n          <input\n            id=\"board-size\"\n            type=\"number\"\n            min={MIN_SIZE}\n            max={MAX_SIZE}\n            value={size}\n            onChange={(e) => onSizeChange(e.target.value)}\n          />\n        </label>\n\n        <p className=\"status\" aria-live=\"polite\">{selectedLabel}</p>\n\n        <div\n          className=\"board\"\n          role=\"grid\"\n          aria-label=\"interactive chessboard\"\n          style={{ gridTemplateColumns: `repeat(${size}, minmax(0, 1fr))` }}\n        >\n          {rows.map((row) =>\n            cols.map((col) => {\n              const isDark = (row + col) % 2 === 1;\n              const key = toKey(row, col);\n              const isActive = selectedKey === key;\n\n              return (\n                <button\n                  key={key}\n                  type=\"button\"\n                  role=\"gridcell\"\n                  className={`cell ${isDark ? 'cell--dark' : 'cell--light'} ${isActive ? 'cell--active' : ''}`}\n                  onClick={() => onSelectCell(row, col)}\n                  aria-label={`Row ${row + 1} Column ${col + 1}`}\n                  aria-pressed={isActive}\n                />\n              );\n            })\n          )}\n        </div>\n      </section>\n    </main>\n  );\n}\n",
    "src/App.css": ":root {\n  color-scheme: dark;\n}\n\nhtml, body {\n  margin: 0;\n  height: 100%;\n  background: #0a0a0a;\n}\n\nbody {\n  font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;\n}\n\n.page {\n  min-height: 100vh;\n  padding: 24px;\n  display: grid;\n  place-items: center;\n  color: #e5e7eb;\n}\n\n.card {\n  width: min(760px, 100%);\n  background: #0b1020;\n  border: 1px solid #1f2937;\n  border-radius: 14px;\n  padding: 18px;\n  display: grid;\n  gap: 14px;\n}\n\nh1 {\n  margin: 0;\n  font-size: 20px;\n}\n\n.subtitle {\n  margin: 0;\n  color: #9ca3af;\n  font-size: 13px;\n}\n\n.size {\n  display: inline-grid;\n  gap: 6px;\n  font-size: 12px;\n  color: #9ca3af;\n  width: max-content;\n}\n\n.size input {\n  width: 90px;\n  background: #0a0f1e;\n  color: #e5e7eb;\n  border: 1px solid #374151;\n  border-radius: 10px;\n  padding: 8px 10px;\n}\n\n.status {\n  margin: 0;\n  padding: 8px 10px;\n  border: 1px solid #1f2937;\n  border-radius: 10px;\n  background: #0a0f1e;\n  font-size: 13px;\n}\n\n.board {\n  width: min(560px, 100%);\n  display: grid;\n  border: 1px solid #374151;\n  border-radius: 12px;\n  overflow: hidden;\n}\n\n.cell {\n  aspect-ratio: 1 / 1;\n  border: none;\n  cursor: pointer;\n  outline: none;\n  box-shadow: inset 0 0 0 1px rgba(15, 23, 42, 0.15);\n  transition: box-shadow 120ms ease, transform 120ms ease;\n}\n\n.cell--light {\n  background: #e5e7eb;\n}\n\n.cell--dark {\n  background: #334155;\n}\n\n.cell:hover {\n  box-shadow: inset 0 0 0 2px rgba(14, 165, 233, 0.65);\n}\n\n.cell--active {\n  box-shadow: inset 0 0 0 3px #facc15 !important;\n}\n\n.cell:focus-visible {\n  box-shadow: inset 0 0 0 3px #a3e635 !important;\n}\n",
    "tsconfig.json": "{\n  \"compilerOptions\": {\n    \"jsx\": \"react-jsx\",\n    \"target\": \"ES2020\",\n    \"module\": \"ESNext\",\n    \"strict\": true\n  }\n}\n"
  }
}
